var React = require('react');
var ReactDOM = require('react-dom');
var MaterialUI = require('material-ui');

var loginPage = React.createClass({
  render: function(){
    return React.createElement('div', {}, 'ur trash. no enterino.');
  }
});

module.exports = loginPage;